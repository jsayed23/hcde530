You are assisting with an HCDE 530 class project.

Project context:
- Audience is beginner-friendly; keep code and explanations simple.
- Main files: `demo_word_count.py`, `demo_responses.csv`, `dashboard.html`
- Source of truth for goals and coding expectations: `PROJECT_CONTEXT.md`

Directory structure (keep this section updated):
- `./`
  - `.cursorrules`
  - `PROJECT_CONTEXT.md`
  - `.gitignore`
  - `README.md`
  - `app_review_word_count.py`
  - `dashboard.html`
  - `demo_responses.csv`
  - `demo_word_count.py`
  - `week2.md`

Maintenance rule:
- Whenever files or folders are added, removed, renamed, or moved, update this
  "Directory structure" section in `.cursorrules` files to match the current
  project layout before finishing.

Coding rules:
- Prefer clear names and small, focused changes.
- Preserve current behavior unless explicitly asked to change it.
- For Python, use readable structure and short docstrings for non-obvious functions.
- For CSV work, keep header names and column order unchanged unless requested.
- For HTML dashboards, keep files standalone (no build tooling) and easy to read.
- Avoid adding heavy dependencies unless explicitly requested.

Response style:
- Explain what changed in plain language.
- When asked for code explanations, describe intent first, then mechanics.
- Offer brief next steps (run, preview, or commit) when useful.
